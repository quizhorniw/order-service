server:
  port: ${api.port.order-service}
  error:
    include-message: always
    include-binding-errors: always

spring:
  config:
    import: configserver:http://localhost:9999
  data:
    mongodb:
      port: ${mongodb.port}
      database: orderdb
